# กำหนดว่า item แต่ละตัว "ต้องเช็คแบบไหน" (check pattern) ไม่ใช่ผูกกับ Fabric item type ตรงๆ
# check ที่รองรับตอนนี้: unit_test | data_quality | structure | schema | none
#
# ลำดับการหา check pattern ของแต่ละ item (จากเจาะจงสุด -> กว้างสุด):
#   1. entry เฉพาะ item นั้น (ระบุ check ตรงๆ)              <- override เฉพาะตัว
#   2. _defaults.<fabric_type> ของ item นั้น (ดูจาก .Notebook / .DataPipeline / ... ท้ายชื่อ folder) <- ค่า default ตาม type
#   3. unit_test (fail-safe เข้มสุด ถ้าไม่มีทั้ง 1 และ 2)
#
# ข้อดี: item ที่ pattern ตรงกับ default ของ type ตัวเองอยู่แล้ว "ไม่ต้องเขียน entry เลย"
# เขียน entry เฉพาะตอนที่ต้องการ check pattern ต่างจาก default ของ type, หรือ skip_check เท่านั้น
# ทำให้ไม่ต้องมานั่งเขียน entry ซ้ำๆ ทุกตัวเวลามี item เพิ่มเยอะ
#
# Opt-out รายตัว (ใช้ได้กับทุก pattern รวม structure/schema):
#   skip_check: true
#   skip_reason: "<เหตุผลที่ต้องระบุ ไม่ปล่อยผ่านแบบเงียบๆ>"
# ต่างจาก check: none ตรงที่ยังคง label ว่า item นี้ "ควรจะ" เช็คแบบไหนไว้ (เผื่อกลับมาเปิดทีหลัง)
#
# ข้อดี: Fabric ออก item type ใหม่กี่ตัวก็ตาม (Eventstream, KQL Database, Reflex, ...)
# แค่เพิ่ม default ที่นี่ (หรือ entry เฉพาะตัวถ้าจำเป็น) แล้วแม็ปไปยัง check pattern ที่มีอยู่แล้ว
# ไม่ต้องแก้ fabric-ci.yml เลย

_defaults:
  Notebook: unit_test
  DataPipeline: structure
  Lakehouse: schema
  Warehouse: schema
# --- Override เฉพาะ item ที่ต้องการ pattern ต่างจาก default ของ type, หรือ skip_check ---

df_test_dataflow:
  skip_check: true
  skip_reason: >-
    Dataflow Gen2 test item (2026-09-07) สำหรับทดลอง Copy Data ต่อยอด — ยังไม่มี validator
    ที่เข้าใจ format ของ Dataflow (mashup.pq / queryMetadata.json ต่างจาก pipeline-content.json
    ของ DataPipeline หรือ table schema ของ Lakehouse/Warehouse โดยสิ้นเชิง) เขียน validator ที่
    เหมาะสมแล้วค่อยเอา skip_check ออก

nb_yayee_sales_transform:
  skip_check: true
  skip_reason: >-
    Demo scenario item (2026-09-09) — เนื้อหาเป็นโค้ดระดับ Spark table (spark.table("sales")
    .withColumn(...).saveAsTable(...)) แก้ live ตอน demo team collaboration ไม่ใช่ pure function
    เหมือน calculate_regional_sales ตัวเดิม เลยไม่มี unit test ที่ทดสอบได้จริงแบบ isolated
    (ต้องใช้ Spark session จริง) เขียน integration test ให้เหมาะสมแล้วค่อยเอา skip_check ออก

nb_fern_dq_check:
  skip_check: true
  skip_reason: >-
    Demo scenario item (2026-09-09) — Great Expectations checkpoint ที่ assert ตรงบน
    spark.table("inventory") ผ่าน Notebook Activity ของ pl_fern_inventory_dq ไม่ใช่ pure
    function เหมือนกัน ต้องมี Spark session + great_expectations จริงถึงจะรันทดสอบได้
    เขียน integration test ให้เหมาะสมแล้วค่อยเอา skip_check ออก